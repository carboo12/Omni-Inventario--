// GENERADO AUTOMÁTICAMENTE. No editar.
// Wrappers de transporte para el módulo 'export' (llaman a la API).
import { callAction } from '../api-client';

export async function exportInventoryToExcel(...args: any[]): Promise<any> {
  return callAction('export', 'exportInventoryToExcel', args);
}
